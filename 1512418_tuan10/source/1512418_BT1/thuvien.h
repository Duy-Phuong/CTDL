#include <iostream>
using namespace std;
#include <fstream>
#include < string>
#include <vector>
#include <ctime>

void docFile(int a, string s, string s1, string s2);
int Brute_Force(string source, string substr);

int KMP(string source, string T);
